# Development dependencies for tiff-reducer
# Install with: pip install -r requirements-dev.txt

# Linting and formatting
ruff>=0.4.0

# Testing dependencies (for Python test scripts)
# Note: NumPy <2.0 required for GDAL compatibility
numpy>=1.24.0,<2.0.0
pillow>=10.0.0
